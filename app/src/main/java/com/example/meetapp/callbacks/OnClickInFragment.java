package com.example.meetapp.callbacks;

public interface OnClickInFragment {
    /**
     * a method called in an activity when a click has happened in some fragment
     * @param action represent the action that is needed
     */
    void onClickInFragment(String action);
}
